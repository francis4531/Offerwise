# Development / Testing dependencies (NOT deployed to production)
# Install: pip install -r requirements-dev.txt

-r requirements.txt

# Testing
fpdf2==2.8.2          # Generate synthetic PDFs for adversarial tests
pytest>=7.0
ruff>=0.4.0

# Reddit agent (development only)
praw==7.7.1
